package main

import (
	"github.com/containernetworking/cni/pkg/types"
	"fmt"
)

func GetDns(dnsEtcdConfig *map[string]string)(*types.DNS){
	//var DnsRoad *EtcdConfig
	//var dns *types.DNS
	//dns = &types.DNS{}

	//fmt.Printf("length:%v ",len(*DnsRoad.Dns))
	//if _, ok := (*KeyRoad)["domain"]; !ok {
	//	dns.Domain = (*KeyInfo)["domain"]
	//
	//}

	//if _, ok := (*KeyInfo)["nameservers"]; !ok {
	//	var nameservers []string = strings.Split((*KeyInfo)["nameservers"], ",")
	//	dns.Nameservers = nameservers
	//
	//}
	//
	//
	//if _, ok := (*KeyInfo)["search"]; !ok {
	//	var search []string = strings.Split((*KeyInfo)["search"], ",")
	//	dns.Search = search
	//}
	//
	//if _, ok := (*KeyInfo)["options"]; !ok {
	//	var options []string = strings.Split((*KeyInfo)["options"], ",")
	//	dns.Options = options
	//}
	//return dns

}